# Bookstore Management & Interaction System

## Build Instructions

1. Open a terminal and navigate to the project root folder.

2. Create a build directory and compile:

```bash
mkdir build
cd build
cmake ..
make